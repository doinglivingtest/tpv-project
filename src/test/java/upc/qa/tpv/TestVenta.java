package upc.qa.tpv;

import static org.junit.Assert.assertEquals;

import org.junit.Before;
import org.junit.Test;

import java.util.List;

public class TestVenta {
/*
            List<Codigo> codes;
            Venta venta;
            long lastCode = 777852454;
            @Before
            public void init(){
                codes.add(new Codigo(899898989));
                codes.add(new Codigo(899898232));
                codes.add(new Codigo(899844432));
                venta = new Venta();

            }

            @Test
            public void testAddCode(){
                venta.addCodigo(new Codigo(lastCode));

                assertEquals(lastCode, venta.returnVentas().get(3));
            }
            */

}
